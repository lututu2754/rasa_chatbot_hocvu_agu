# File: credentials.yml

# rest:
#   # Bật CORS, cho phép mọi nguồn ("*") kết nối
#   cors_origins: ["*"]

# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
#   # Bật CORS cho kết nối real-time
#   cors_origins: ["*"]

# (Các credentials khác nếu có, ví dụ rasa enterprise)
rasa:
  url: "http://localhost:5002/api"